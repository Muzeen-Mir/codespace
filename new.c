#include<stdio.h>
int main()
{
    int a,b;
    printf("enter the number\n");
    scanf("%d%d",&a,&b);
    if(a>b)
    (
        printf("a is greater than b");
    )
    else if(a=b)
    (
        printf("a is equal to b");
    )
    else
    printf("b is greater than a");
    return 0;
}